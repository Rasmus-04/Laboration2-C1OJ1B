<UserControl x:Class="Laboration_2.View.UserControls.GameListView.GameListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Laboration_2.View.UserControls.GameListView"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <ListView ItemsSource="{Binding AllaSpel}" Name="lvSpel" SelectionMode="Single" SelectedItem="{Binding SelectedGameItem}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Titel" DisplayMemberBinding="{Binding Titel}" Width="75"/>
                    <GridViewColumn Header="Max spelare" DisplayMemberBinding="{Binding MaxPlayers}" Width="75"/>
                    <GridViewColumn Header="Min spelare" DisplayMemberBinding="{Binding MinPlayers}" Width="75" />
                </GridView>
            </ListView.View>
        </ListView>


        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="5">
            <Button Content="Ta bort Spel" Name="btnRemoveGame" Command="{Binding btnRemoveGame}"/>
        </StackPanel>
    </Grid>
</UserControl>
